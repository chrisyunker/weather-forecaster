[project]
name = "weather-forecaster"
version = "0.1.0"
description = "Weather forcaster app"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "apscheduler>=3.11.2",
    "fastapi[standard]>=0.136.0",
    "requests>=2.33.1",
]
